{
  "name": "@malloc72p/prepared-prisma",
  "version": "1.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:dev": "prisma migrate dev",
    "db:production": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^4.7.1"
  },
  "devDependencies": {
    "prisma": "^4.7.1",
    "@malloc72p/eslint-config-malloc72p": "1.0.0"
  }
}
